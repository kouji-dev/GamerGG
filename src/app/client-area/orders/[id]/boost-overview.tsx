import {FC} from "react";
import {Card} from "@/ui/Card";

type BoostOverviewProps = {}

export const BoostOverview: FC<BoostOverviewProps> = (props) => {

    return (
        <Card>Overview</Card>
    )
}